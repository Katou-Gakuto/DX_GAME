@startuml
'--------------------
' モデル関連
'--------------------
class モデル基礎 {
    - mvサイズ : VECTOR
    - mv位置 : VECTOR
    - mv角度 : VECTOR
    - mpモデルコントローラー : モデル達基礎
    - mb描画フラグ : bool
    + 初期化()
    + 終了()
    + ゲーム初期化()
    + ポジション更新()
    + モデル描画() 
    + GetSize() : VECTOR
    + SetSize(VECTOR)
    + GetPosition() : VECTOR
    + SetPosition(VECTOR)
    + GetAngle() : VECTOR
    + SetAngle(VECTOR)
    + SetModelsController(モデル達基礎*)
}

class モデル達基礎 {
    - mpモデルリスト : std::vector<モデル基礎*>
    - mvモデル位置 : VECTOR
    - mvモデル角度 : VECTOR
    - mvモデルサイズ : VECTOR
    - mbモデル描画フラグ : bool
    + 初期化()
    + 終了()
    + ゲーム初期化(VECTOR, VECTOR, VECTOR)
    + モデル追加(モデル基礎*)
    + モデル位置設定(...)
    + モデル更新()
    + モデル描画()
}

モデル基礎 --> モデル達基礎 : 所属

'--------------------
' アニメーション関連
'--------------------
class アニメーションデータ {
    - animDatas : std::map<ANIMATION_TYPE, 一つのアニメーションデータ>
}

class 一つのアニメーションデータ {
    - 前のアニメーションタイプ : ANIMATION_TYPE
    - ループフラグ : bool
    - モデルタイプ : MODEL_TYPE
    - アニメーションカウント : float
    - アニメーションハンドル : int
    - ブレンドモード : int
    - ブレンドパラメータ : int
}

class アニメーション基礎 {
    - mpモデルコントローラー : モデル達基礎*
    - mpFSM : アニメーションFSM*
    - mstアニメーションデータ : std::vector<アニメーションデータ*>
    - mmアニメーション時間 : std::map<ANIMATION_TYPE, int>
    + 初期化()
    + シーン最終初期化()
    + 終了()
    + 更新()
}

アニメーション基礎 --> モデル達基礎 : 使用
アニメーション基礎 --> アニメーションFSM : 使用
アニメーション基礎 --> アニメーションデータ : 所持

'--------------------
' FSM関連
'--------------------
class FSM基礎<ステート番号, ステート> {
    - mmステートマップ : std::map<ステート番号, ステート*>
    - mn現在ステート : ステート番号
    - mn次ステート : ステート番号
    + 登録ステート(ステート)
    + 登録ステート(ステート番号, ステート)
    + 初期化()
    + 現在ステート取得()
}

class アニメーションFSM {
    - mmアニメーションステート : std::vector<std::map<MODEL_TYPE, アニメーションステート*>>
    + 初期化(アニメーション基礎*)
    + 終了(アニメーション基礎*)
    + 更新(アニメーション基礎*, std::vector<アニメーションデータ*>)
    + 次ステート設定(ANIMATION_TYPE)
}

class アニメーションステート基礎 {
    - mpモデル基礎 : モデル基礎*
    + 入る(アニメーション基礎*, 一つのアニメーションデータ*, アニメーションデータ*, MODEL_TYPE)
    + 出る(アニメーション基礎*, 一つのアニメーションデータ*, アニメーションデータ*, MODEL_TYPE)
    + 更新(アニメーション基礎*, 一つのアニメーションデータ*)
    + 終了(アニメーション基礎*, アニメーションデータ*)
    + 同じモデルタイプ確認(MODEL_TYPE) : bool
}

class アニメーション操作ステート基礎 {
    + 入る(アニメーション基礎*, ANIMATION_TYPE)
    + 出る(アニメーション基礎*, ANIMATION_TYPE)
    + ステート確認(アニメーション基礎*, ANIMATION_TYPE) : ANIMATION_TYPE
}

アニメーションFSM --> アニメーション基礎 : 操作
アニメーションFSM --> アニメーションステート基礎 : 使用
アニメーションステート基礎 --> モデル基礎 : 所属
アニメーション操作ステート基礎 --> アニメーション基礎 : 使用

@enduml
